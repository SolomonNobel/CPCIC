

<div class="image-container">
  <img src="back (2).jpeg" alt="Image 1" class="pop-slide">
  <img src="back (2).jpeg" alt="Image 2" class="pop-slide">
  <img src="back (2).jpeg" alt="Image 3" class="pop-slide">
</div>
 

aayoozi




.image-container {
  display: flex;
  justify-content: space-around;
  align-items: center;
  gap: 20px;
  margin-top: 50px;
}

.pop-slide {
  width: 200px;           /* adjust size */
  height: auto;
  opacity: 0;
  transform: translateY(50px) scale(0.8);
  animation: slidePop 0.8s forwards;
}

/* Staggered animation delays */
.pop-slide:nth-child(1) { animation-delay: 0s; }
.pop-slide:nth-child(2) { animation-delay: 0.3s; }
.pop-slide:nth-child(3) { animation-delay: 0.6s; }

/* Keyframes for pop + slide */
@keyframes slidePop {
  0% {
    opacity: 0;
    transform: translateY(50px) scale(0.8);
  }
  60% {
    opacity: 1;
    transform: translateY(-10px) scale(1.05);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
















elowwwwwwwwwwwwwwwwwwwwwwww

.image-slider {
  position: fixed;       /* stay fixed behind header */
  top: 0;
  left: 0;
  width: 100vw;          /* full viewport width */
  height: 100vh;         /* full viewport height */
  z-index: -1;            /* behind header */
  overflow: hidden;
}
/* Optional: Adjust the navigation items to be visible above the image */
nav {
  z-index: 2; /* Ensure that the navigation items are on top of the images */
}

.slider-img {
  position: absolute;
  width: 100%;
  height: 50%;
  object-fit: cover;
  opacity: 0;
  transform: scale(0.8) translateY(30px);
  animation: popSlide 9s infinite;
}

/* Fullscreen slider behind header */
.image-slider {
  position: fixed;       /* stay full screen */
  top: 0;
  left: 0;
  width: 100vw;
  
  z-index: 0;            /* behind header */
  overflow: hidden;
}

/* Slider images */
/* Slider images */
.slider-img {
  position: absolute;
  width: 100%;
  height:100%;
  object-fit: cover;      /* fully cover the container */
  top: 0;
  left: 0;
  opacity: 0;
  filter: brightness(50%); 
  transform: scale(1);
  animation: fastPop 6s infinite;  /* total cycle: 6s for 3 images → 2s each */
}

/* Staggered delays for each image */
.slider-img:nth-child(1) { animation-delay: 0s; }
.slider-img:nth-child(2) { animation-delay: 2s; }


/* Fast pop + fade keyframes */
@keyframes fastPop {
  0% {
    opacity: 0;
    transform: scale(1);
  }
  10% {
    opacity: 1;
    transform: scale(1.05);
  }
  80% {
    opacity: 1;
    transform: scale(1);
  }
  90% {
    opacity: 0;
    transform: scale(1);
  }
  100% {
    opacity: 0;
    transform: scale(1);
  }
}
